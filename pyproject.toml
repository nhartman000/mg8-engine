[project]
name = "mg8-engine"
version = "0.1.0"
description = "Deterministic logic-gated execution engine with Nych symbolic determinism"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.8",
    "click>=8.1",
    "pyyaml>=6.0",
    "python-dateutil",
]

[project.scripts]
mg8-run = "cli.mg8_run:cli"
