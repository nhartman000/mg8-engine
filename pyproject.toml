
[project]
name = "mg8-engine"
version = "0.1.0"
description = "Deterministic logic-gated execution engine for composable intelligence units with Nych integration"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "click>=8.0",
    "pyyaml",           # for easier .ork if we want human-readable
]

[project.scripts]
mg8-run = "cli.mg8_run:cli"
